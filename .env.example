# .env.example
# Copy to .env and fill in real values. Never commit .env to source control.

# ---------------------------------------------------------------
# EXISTING — First Command (unchanged)
# ---------------------------------------------------------------

# Main application database (teams, reports, memberships)
DATABASE_URL=postgresql://user:password@host:5432/firstcommand

# Basic-auth credentials for file/API routes
API_USERNAME=admin
API_PASSWORD=changeme

# FRC API credentials
FRC_API_USERNAME=your_frc_username
FRC_API_TOKEN=your_frc_token

# Server port
PORT=5000

# ---------------------------------------------------------------
# NEW — Auth system (separate infrastructure)
# ---------------------------------------------------------------

# Dedicated PostgreSQL database for auth_users table only
# Provision a second DB instance and paste its connection string here
AUTH_DATABASE_URL=postgresql://user:password@host:5432/firstcommand_auth

# Dedicated Redis instance for refresh token storage only
# Defaults to redis://localhost:6379 if not set
AUTH_REDIS_URL=redis://user:password@host:6379

# Secret key used to sign JWTs — use a long random string in production
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
AUTH_JWT_SECRET=replace_with_a_long_random_secret
